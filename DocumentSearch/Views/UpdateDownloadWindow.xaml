<Window x:Class="DocumentSearch.Views.UpdateDownloadWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Güncelleme İndiriliyor" 
        Height="200" 
        Width="400"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent">
    <Border Background="White" 
            CornerRadius="10"
            BorderBrush="#E0E0E0"
            BorderThickness="1"
            Padding="20">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Başlık -->
            <TextBlock Grid.Row="0" 
                       Text="Güncelleme İndiriliyor..." 
                       FontSize="16" 
                       FontWeight="Bold"
                       Margin="0,0,0,15"
                       HorizontalAlignment="Center"/>

            <!-- Progress Bar -->
            <ProgressBar Grid.Row="1"
                         Value="{Binding DownloadProgress}"
                         Maximum="100"
                         Height="25"
                         Margin="0,0,0,10"/>

            <!-- Yüzde Bilgisi -->
            <TextBlock Grid.Row="2"
                       Text="{Binding ProgressText}"
                       FontSize="12"
                       Foreground="#666"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,10"/>

            <!-- Durum Mesajı -->
            <TextBlock Grid.Row="3"
                       Text="{Binding StatusMessage}"
                       FontSize="11"
                       Foreground="#999"
                       HorizontalAlignment="Center"
                       TextWrapping="Wrap"/>
        </Grid>
    </Border>
</Window>

